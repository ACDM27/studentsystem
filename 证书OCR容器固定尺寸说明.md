# 证书OCR容器固定尺寸设置说明

## 修改时间
2026-02-05

## 修改目标
将模态框中的左右面板从**相对百分比**改为**固定像素值**，确保容器尺寸严格控制，不会超出父容器 `n-card__content` 的范围。

## 容器层级结构

```
.verify_modal (n-modal)
  └── .n-card__content (最大 800px 高度)
      └── .verify_container (padding: 20px, gap: 20px)
          ├── .left_panel (固定 900px 宽)
          └── .right_panel (固定 500px 宽)
```

## 固定尺寸设置

### 1. 父容器 `.n-card__content`
```css
.verify_modal :deep(.n-card__content) {
  height: 80vh;
  max-height: 800px;  /* 最大高度 */
  min-height: 600px;  /* 最小高度 */
}
```

### 2. 内部容器 `.verify_container`
```css
.verify_container {
  height: 100%;       /* 填满父容器 */
  width: 100%;
  padding: 20px;      /* 四周内边距 */
  gap: 20px;          /* 左右面板间距 */
  box-sizing: border-box;
  overflow: hidden;   /* 严格隐藏溢出 */
}
```

### 3. 左侧图片面板 `.left_panel` ✅
```css
.left_panel {
  flex: 0 0 auto;                        /* 不使用百分比 */
  width: 900px;                          /* 固定宽度 900px */
  max-width: calc(100% - 520px);         /* 动态最大值，确保不超出 */
  height: 100%;                          /* 填满父容器高度 */
  max-height: 100%;                      /* 严格限制最大高度 */
  overflow: hidden;                      /* 隐藏溢出内容 */
}
```

**计算说明**：
- `max-width: calc(100% - 520px)` 
  - 520px = right_panel (500px) + gap (20px)
  - 确保在小屏幕上自动调整

### 4. 右侧表单面板 `.right_panel` ✅
```css
.right_panel {
  flex: 0 0 auto;      /* 不使用百分比 */
  width: 500px;        /* 固定宽度 500px */
  max-width: 500px;    /* 严格限制最大宽度 */
  height: 100%;        /* 填满父容器高度 */
  max-height: 100%;    /* 严格限制最大高度 */
  overflow: hidden;    /* 外层隐藏溢出 */
}
```

## 尺寸计算验证

### 宽度验证
```
verify_container 总宽度 = 左侧面板 + 间距 + 右侧面板 + 左右padding
                       = 900px + 20px + 500px + 40px
                       = 1460px
```

✅ **模态框最大宽度 = 1600px**，留有 140px 余量

### 高度验证
```
verify_container 总高度 = 父容器高度 100%
左右面板高度 = 父容器高度 100%
```

✅ 高度通过 `height: 100%` 和 `max-height: 100%` 严格限制

## 溢出控制策略

### 多层防护
1. **外层**: `.verify_modal :deep(.n-card__content)` - `overflow: hidden`
2. **容器层**: `.verify_container` - `overflow: hidden`
3. **面板层**: `.left_panel`, `.right_panel` - `overflow: hidden`
4. **内容层**: `.right_panel :deep(.n-form)` - `overflow-y: auto` (仅表单内容可滚动)

### 证书图片控制
- **图片视口**: `padding: 20px` - 留出安全边距
- **图片画布**: `max-width: 100%`, `max-height: 100%`, `overflow: hidden`
- **图片本身**: `max-width: 90%`, `max-height: 90%` - 为旋转留空间

## 优势

### ✅ 固定像素值的好处
1. **精确控制**: 尺寸完全可预测
2. **不会超出**: 严格限制在父容器范围内
3. **一致性**: 在不同场景下保持相同尺寸
4. **调试方便**: 更容易定位布局问题

### ✅ 使用 calc() 的好处
1. **响应式兜底**: 小屏幕时自动调整
2. **防止溢出**: 动态计算确保总宽度不超出
3. **灵活性**: 保留一定的适应能力

## 测试场景

请测试以下场景确保不溢出：

### 场景1：正常显示
- 打开模态框
- 检查左侧图片面板宽度为 900px
- 检查右侧表单面板宽度为 500px
- 总宽度不超出模态框

### 场景2：缩放操作
- 放大证书图片
- 旋转证书（90°/180°/270°）
- 拖拽证书位置
- 确保图片不超出左侧面板边界

### 场景3：表单滚动
- 填写多个表单字段
- 检查右侧表单超出时出现滚动条
- 确保表单不超出右侧面板边界

### 场景4：浏览器窗口调整
- 缩小浏览器窗口
- 检查是否使用 `max-width: calc(100% - 520px)` 自动调整
- 确保在小窗口下也不溢出

## 后续优化建议

如果需要支持更多屏幕尺寸，可以考虑：

### 媒体查询
```css
/* 大屏幕 (>1600px) */
@media (min-width: 1600px) {
  .left_panel { width: 1000px; }
  .right_panel { width: 550px; }
}

/* 中等屏幕 (1200px - 1600px) */
@media (max-width: 1600px) {
  .left_panel { width: 900px; }
  .right_panel { width: 500px; }
}

/* 小屏幕 (<1200px) */
@media (max-width: 1200px) {
  .left_panel { width: 700px; }
  .right_panel { width: 400px; }
}
```

## 总结

- ✅ 左侧面板固定 **900px**
- ✅ 右侧面板固定 **500px**
- ✅ 间距固定 **20px**
- ✅ 总宽度 **1460px** (含padding)
- ✅ 严格不超出父容器 **1600px**
- ✅ 使用 `calc()` 动态兜底
- ✅ 多层 `overflow: hidden` 防护

**所有尺寸现在都是固定的像素值，完全可控！** 🎉
