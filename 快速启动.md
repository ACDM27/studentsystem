# 快速启动指南

## 🚀 最简单的方式（推荐）

### 一键启动整个项目

1. **双击运行**：`一键启动.ps1`

   或者在项目根目录执行：
   ```powershell
   .\一键启动.ps1
   ```

2. **访问应用**
   - 前端应用：http://localhost:5173
   - 后端API：http://localhost:8000
   - API文档：http://localhost:8000/docs

3. **测试登录**
   ```
   学号：student001
   密码：password123
   ```

---

## 📝 分步启动（手动控制）

### 第一步：启动后端

**方式 1：使用启动脚本（推荐）**
```powershell
cd backend
.\启动后端.ps1
```

**方式 2：手动启动**
```powershell
cd backend
# 激活虚拟环境（如果有）
..\.venv\Scripts\Activate.ps1
# 启动服务器
python main.py
```

✅ **验证成功**：访问 http://localhost:8000/docs 能看到API文档

---

### 第二步：启动前端

**打开新的终端窗口**

**方式 1：使用启动脚本（推荐）**
```powershell
cd frontend
.\启动前端.ps1
```

**方式 2：手动启动**
```powershell
cd frontend
pnpm run dev
```

✅ **验证成功**：访问 http://localhost:5173 能看到登录页面

---

## 🔧 首次运行前的准备

### 1. 安装 MySQL

确保 MySQL 已安装并运行：
```powershell
# 检查服务
Get-Service MySQL80

# 如果未运行，启动服务
Start-Service MySQL80
```

### 2. 创建数据库

```sql
CREATE DATABASE student_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 3. 配置后端环境变量

编辑 `backend/.env`，修改数据库密码：
```env
DATABASE_URL=mysql+pymysql://root:你的密码@localhost:3306/student_system
```

### 4. 安装 Python 依赖

```powershell
cd backend
pip install -r requirements.txt
```

### 5. 初始化数据库（首次运行）

```powershell
cd backend
python init_db.py
```

### 6. 安装前端依赖

```powershell
cd frontend
pnpm install
```

---

## 🎯 端口配置总结

| 服务     | 端口  | 访问地址                    |
| -------- | ----- | --------------------------- |
| 前端     | 5173  | http://localhost:5173       |
| 后端API  | 8000  | http://localhost:8000       |
| MySQL    | 3306  | localhost:3306              |

**重要**：前端和后端的端口已在配置文件中正确设置：
- `frontend/.env` → `VITE_API_URL=http://localhost:8000`
- `backend/.env` → `ALLOWED_ORIGINS=...http://localhost:5173`

---

## 🐛 常见问题快速解决

### ❌ MySQL 连接失败

**检查清单**：
1. MySQL 服务是否运行？`Get-Service MySQL80`
2. 数据库是否存在？
3. `.env` 中的密码是否正确？

### ❌ 端口被占用

```powershell
# 查找占用进程
netstat -ano | findstr :8000
# 终止进程
taskkill /PID <进程ID> /F
```

### ❌ 前端无法连接后端

1. 确认后端已启动（访问 http://localhost:8000/health）
2. 检查 `frontend/.env` 中的 API 地址是否为 `http://localhost:8000`
3. 清除浏览器缓存

### ❌ pnpm 命令不存在

```powershell
npm install -g pnpm
```

---

## 🛑 停止服务

- **一键启动方式**：关闭弹出的两个PowerShell窗口
- **手动启动方式**：在各自终端按 `Ctrl + C`

---

## 📚 完整文档

详细信息请参考：**项目运行指南.md**

---

**最后更新**：2026-01-22
