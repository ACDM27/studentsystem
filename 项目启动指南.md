# 🚀 项目启动指南

## ⚡ 快速启动（推荐方式）

### 方法1：使用PowerShellscripts（最简单）

#### 1. 启动后端
```powershell
cd d:\student_system\backend
.\启动后端.ps1
```

#### 2. 启动前端（新开一个PowerShell窗口）
```powershell
cd d:\student_system\frontend
.\启动前端.ps1
```

#### 3. 访问系统
打开浏览器访问: **http://localhost:5173**

#### 4. 登录测试
- 用户名: `admin`
- 密码: `admin`

---

## 🛠️ 手动启动方式

### 启动后端

```powershell
# 1. 进入后端目录
cd d:\student_system\backend

# 2. 确保依赖已安装
pip install -r requirements.txt

# 3. 启动服务
python -m uvicorn main:app --reload
```

**后端启动成功标志**：
```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete.
```

### 启动前端

```powershell
# 1. 进入前端目录  
cd d:\student_system\frontend

# 2. 确保依赖已安装（首次运行）
npm install --force

# 3. 启动开发服务器
npm run dev
```

**前端启动成功标志**：
```
VITE v5.4.x  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

---

## 📋 启动前检查清单

### 必要条件
- [ ] MySQL服务已启动
- [ ] Python依赖已安装
- [ ] Node.js依赖已安装
- [ ] 数据库student_system已创建
- [ ] 测试账号已创建

### 验证命令

#### 检查MySQL服务
```powershell
Get-Service MySQL* | Select-Object Name, Status
# 状态应该是 Running
```

#### 检查Python依赖
```powershell
cd d:\student_system\backend
python -c "import fastapi, uvicorn, pymysql; print('依赖OK')"
```

#### 检查Node.js版本
```powershell
node --version  # 应该 >= 16.x
npm --version
```

---

## 🎯 服务地址

| 服务 | 地址 | 说明 |
|------|------|------|
| **前端** | http://localhost:5173 | 用户界面 |
| **后端API** | http://localhost:8000 | API服务 |
| **API文档** | http://localhost:8000/docs | Swagger文档 |

---

## 🎓 测试账号

| 用户名 | 密码 | 角色 | 说明 |
|--------|------|------|------|
| admin | admin | 学生 | **推荐使用** |
| sysadmin | admin123 | 管理员 | 系统管理 |
| student001 | password123 | 学生 | 张三 |
| student002 | password123 | 学生 | 李四 |

---

## ❌ 常见错误解决

### 错误1: 前端无法访问

**症状**: 浏览器显示"无法访问此网站"

**解决**:
1. 确认前端服务已启动并显示 "Local: http://localhost:5173/"
2. 检查5173端口是否被占用：
   ```powershell
   netstat -ano | findstr :5173
   ```
3. 如果被占用，停止占用进程或更改端口

### 错误2: 后端连接失败  

**症状**: 前端显示网络错误或404

**解决**:
1. 确认后端服务已启动在8000端口
2. 访问 http://localhost:8000/docs 测试后端是否正常
3. 检查浏览器控制台的网络请求

### 错误3: Module not found

**症状**: `Cannot find package 'vite'` 或类似错误

**解决**:
```powershell
cd d:\student_system\frontend
Remove-Item node_modules -Recurse -Force
Remove-Item package-lock.json -Force
npm install --force
```

### 错误4: 数据库连接失败

**症状**: `Can't connect to MySQL server`

**解决**:
1. 启动MySQL服务：
   ```powershell
   net start MySQL
   ```
2. 检查.env配置是否正确
3. 确认密码：root/password067517

### 错误5: 登录失败

**症状**: 用户名或密码错误

**解决**:
1. 确认使用正确的测试账号：admin/admin
2. 检查数据库中是否有账号：
   ```sql
   USE student_system;
   SELECT username, role FROM sys_users;
   ```
3. 如果没有，运行：
   ```powershell
   cd d:\student_system\backend
   python init_db.py
   ```

---

## 🔄 重启服务

### 重启后端
在后端终端按 `Ctrl+C`，然后重新运行：
```powershell
python -m uvicorn main:app --reload
```

### 重启前端
在前端终端按 `Ctrl+C`，然后重新运行：
```powershell
npm run dev
```

---

## 📝 开发提示

### 热重载
- **后端**: 使用 `--reload` 参数，代码修改后自动重启
- **前端**: Vite自动热更新，保存后立即生效

### 查看日志
- **后端**: 直接在终端查看uvicorn输出
- **前端**: 浏览器F12开发者工具 → Console

### API测试
访问 http://localhost:8000/docs 使用Swagger UI测试API

---

## 🎉 启动成功标志

当看到以下界面和信息时，说明启动成功：

### 后端成功
```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete.
```

### 前端成功  
```
➜  Local:   http://localhost:5173/
```

### 访问成功
- 浏览器能打开 http://localhost:5173
- 显示登录页面
- 能使用admin/admin登录

---

**准备就绪！开始使用系统吧！** 🚀
