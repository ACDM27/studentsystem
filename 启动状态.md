# 项目启动状态

## ✅ 启动脚本已执行

执行时间：2026-01-22 10:45

### 🚀 已启动的服务

#### 📋 启动流程
1. ✓ 启动脚本运行中：`StartAll.ps1`
2. ⏳ 检查 MySQL 服务状态
3. ⏳ 在新窗口启动后端（FastAPI on port 8000）
4. ⏳ 在新窗口启动前端（Vite on port 5173）

### 🌐 服务访问地址

| 服务 | 地址 | 状态 |
|------|------|------|
| 🎨 **前端应用** | http://localhost:5173 | ⏳ 启动中 |
| ⚙️ **后端 API** | http://localhost:8000 | ⏳ 启动中 |
| 📚 **API 文档** | http://localhost:8000/docs | ⏳ 启动中 |

### ⏱️ 预计启动时间

- **后端**：约 10-15 秒
  - 数据库连接初始化
  - 加载所有路由和中间件
  - Uvicorn 服务器启动

- **前端**：约 5-10 秒
  - pnpm 依赖检查
  - Vite 开发服务器启动
  - 模块热更新准备

### 🔍 如何验证启动成功

#### 方法 1：检查终端窗口

应该会弹出**两个新的 PowerShell 窗口**：

**后端窗口应显示**：
```
========================================
   Starting Backend (FastAPI + MySQL)   
========================================

[1/4] Checking Python virtual environment...
[OK] Activating local venv...

[2/4] Checking dependencies...
[OK] All dependencies installed

[3/4] Checking configuration...
[OK] Configuration file exists

[4/4] Starting server...
Backend will run at: http://localhost:8000
API docs available at: http://localhost:8000/docs

Initializing database...
Database initialized successfully!
INFO:     Uvicorn running on http://0.0.0.0:8000
```

**前端窗口应显示**：
```
========================================
   Starting Frontend (Vue 3 + Vite)     
========================================

[1/4] Checking Node.js...
[OK] Node.js version: v20.13.1

[2/4] Checking pnpm...
[OK] pnpm version: 9.x.x

[3/4] Checking project dependencies...
[OK] Dependencies installed

[4/4] Starting dev server...

VITE v5.4.11  ready in XXX ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

#### 方法 2：访问浏览器

**等待 15-20 秒后**，打开浏览器访问：

1. **前端应用**：http://localhost:5173
   - 应该看到登录页面
   - Vue DevTools 应该可以检测到 Vue 应用

2. **后端健康检查**：http://localhost:8000/health
   - 应该返回：`{"status":"healthy"}`

3. **API 文档**：http://localhost:8000/docs
   - 应该看到 Swagger UI 界面
   - 显示所有可用的 API 端点

#### 方法 3：检查进程

```powershell
# 检查后端进程
Get-Process | Where-Object {$_.ProcessName -like "*python*"}

# 检查端口占用
netstat -ano | findstr "8000"
netstat -ano | findstr "5173"
```

### 🔐 测试登录

访问 http://localhost:5173 后，使用以下账号登录：

```
学号/用户名：student001
密码：password123
```

或管理员账号：
```
用户名：admin
密码：admin123
```

### ⚠️ 常见问题

#### 问题 1：MySQL 连接失败

**现象**：后端窗口显示数据库连接错误

**解决方案**：
```powershell
# 检查 MySQL 服务
Get-Service MySQL80

# 如果未运行，启动它
Start-Service MySQL80
```

#### 问题 2：端口被占用

**现象**：启动失败，提示端口已被使用

**解决方案**：
```powershell
# 查找占用 8000 端口的进程
netstat -ano | findstr :8000

# 终止进程（替换 <PID> 为实际进程 ID）
taskkill /PID <PID> /F
```

#### 问题 3：前端依赖错误

**现象**：前端窗口显示模块找不到

**解决方案**：
```powershell
cd frontend
pnpm install
```

#### 问题 4：窗口没有弹出

**现象**：脚本运行但看不到新窗口

**解决方案**：手动启动
```powershell
# 后端（新终端）
cd backend
python main.py

# 前端（新终端）
cd frontend
pnpm run dev
```

### 🛑 如何停止服务

1. **使用启动脚本启动的**：
   - 直接关闭弹出的两个 PowerShell 窗口

2. **手动启动的**：
   - 在对应终端按 `Ctrl + C`

3. **强制停止**：
   ```powershell
   # 停止所有 Python 进程（小心！）
   Get-Process python | Stop-Process -Force
   
   # 停止 Node 进程
   Get-Process node | Stop-Process -Force
   ```

### 📊 系统要求检查

确保以下服务正常：
- [x] MySQL 8.0+ 运行中
- [x] Python 3.8+ 已安装
- [x] Node.js 18+ 已安装
- [x] pnpm 已安装
- [x] 端口 8000 可用
- [x] 端口 5173 可用

### 🎉 下一步

启动成功后：
1. 打开浏览器访问 http://localhost:5173
2. 使用测试账号登录
3. 开始使用学生综合信息服务平台！

---

**提示**：如果启动过程中遇到任何问题，请查看对应的终端窗口输出，通常会有详细的错误信息。
