# 项目初始化和启动指南

## 快速启动（3步完成）

### 第1步：安装Python依赖

```powershell
# 进入后端目录
cd d:\student_system\backend

# 方案A：使用conda安装（推荐，因为您使用Anaconda）
pip install -r requirements.txt

# 或者方案B：创建虚拟环境（可选）
# python -m venv .venv
# .\.venv\Scripts\activate
# pip install -r requirements.txt
```

### 第2步：创建数据库和测试账号

```powershell
# 确保MySQL服务正在运行
Get-Service MySQL* | Select-Object Name, Status

# 创建数据库（如果不存在）
mysql -u root -p
# 输入密码后执行：
# CREATE DATABASE student_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
# exit

# 初始化数据库表和测试数据
python init_db.py
```

### 第3步：启动项目

#### 启动后端：
```powershell
cd d:\student_system\backend
python -m uvicorn main:app --reload
```

#### 启动前端（新开一个终端）：
```powershell
cd d:\student_system\frontend
npm run dev
```

#### 访问系统：
打开浏览器访问: http://localhost:5173

#### 登录测试：
- 用户名: `admin`
- 密码: `admin`

---

## 🎓 测试账号信息

| 用户名 | 密码 | 角色 | 姓名 | 说明 |
|--------|------|------|------|------|
| admin | admin | 学生 | 测试学生 | **推荐使用** |
| sysadmin | admin123 | 管理员 | 系统管理员 | 管理员权限 |
| student001 | password123 | 学生 | 张三 | 普通学生 |
| student002 | password123 | 学生 | 李四 | 普通学生 |

---

## 📦 依赖说明

### 后端依赖
主要包：
- `fastapi` - Web框架
- `uvicorn` - ASGI服务器
- `sqlalchemy` - ORM
- `pymysql` - MySQL驱动
- `python-jose` - JWT认证
- `bcrypt` - 密码加密
- `openai` - AI对话（兼容通义千问）

### 前端依赖
运行 `npm install` 会自动安装所有依赖。

---

## ⚙️ 环境配置

### 后端配置 (.env)
检查 `backend/.env` 文件：

```env
# 数据库配置
DATABASE_URL=mysql+pymysql://root:password067517@localhost:3306/student_system

# JWT配置
SECRET_KEY=your-secret-key-change-this-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# AI配置（通义千问）
QWEN_API_KEY=sk-3692d8702cf9418f8e982dd35cd27428
QWEN_MODEL_NAME=qwen-plus
```

### 前端配置 (.env.local)
检查 `frontend/.env.local` 文件：

```env
VITE_API_BASE_URL=http://localhost:8000

# 注意：生产环境构建时（如Docker部署），建议在 .env.production 中设置为 /
# VITE_API_BASE_URL=/
```

---

## 🛠️ 常见问题解决

### 问题1: pymysql未找到
**错误**: `ModuleNotFoundError: No module named 'pymysql'`

**解决**:
```powershell
pip install pymysql
# 或安装所有依赖
pip install -r requirements.txt
```

### 问题2: MySQL连接失败
**错误**: `Can't connect to MySQL server`

**解决**:
1. 检查MySQL服务是否启动
2. 检查.env中的数据库密码是否正确
3. 确认数据库student_system已创建

### 问题3: 前端连接失败
**错误**: 前端无法连接后端

**解决**:
1. 确认后端已启动（http://localhost:8000）
2. 检查浏览器控制台错误
3. 确认.env.local配置正确

---

## 📋 完整安装检查清单

- [ ] Python 3.8+ 已安装
- [ ] Node.js 16+ 已安装
- [ ] MySQL 8.0+ 已安装并运行
- [ ] 后端依赖已安装 (`pip install -r requirements.txt`)
- [ ] 前端依赖已安装 (`npm install`)
- [ ] 数据库student_system已创建
- [ ] 后端.env配置正确
- [ ] 前端.env.local配置正确
- [ ] 数据库表已初始化 (`python init_db.py`)
- [ ] 测试账号已创建

---

## 🚀 验证安装成功

### 1. 后端健康检查
访问: http://localhost:8000/docs
应该看到FastAPI的Swagger文档

### 2. 前端页面
访问: http://localhost:5173
应该看到登录页面

### 3. 登录测试
使用admin/admin登录，应该：
- ✅ 登录成功
- ✅ 跳转到仪表板
- ✅ 侧边栏显示"测试学生"

---

**准备工作完成后，即可开始使用系统！**
