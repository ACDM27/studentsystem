# 如何启动项目

## ✅ 解决方案：使用英文版脚本

由于 PowerShell 编码问题，请使用**英文版启动脚本**：

### 🚀 方法一：一键启动（推荐）

```powershell
.\StartAll.ps1
```

### 📋 方法二：分别启动

**后端**（新终端）：
```powershell
cd backend
.\StartBackend.ps1
```

**前端**（新终端）：
```powershell
cd frontend
.\StartFrontend.ps1
```

### ⚡ 方法三：直接命令

**后端**：
```powershell
cd backend
python main.py
```

**前端**（新终端）：
```powershell
cd frontend
pnpm run dev
```

---

## 📝 首次运行检查清单

### 1. MySQL 数据库
```powershell
# 启动 MySQL 服务
Start-Service MySQL80

# 创建数据库
mysql -u root -p
CREATE DATABASE student_system CHARACTER SET utf8mb4;
```

### 2. 后端配置
编辑 `backend/.env`，修改数据库密码：
```env
DATABASE_URL=mysql+pymysql://root:你的密码@localhost:3306/student_system
```

### 3. 安装依赖

**后端**：
```powershell
cd backend
pip install -r requirements.txt
python init_db.py
```

**前端**：
```powershell
cd frontend
pnpm install
```

---

## 🌐 访问地址

| 服务 | 地址 |
|------|------|
| 前端 | http://localhost:5173 |
| 后端 | http://localhost:8000 |
| API文档 | http://localhost:8000/docs |

**测试账号**：
- 学号：`student001`
- 密码：`password123`

---

## 📂 启动脚本对照表

| 中文脚本名 | 英文脚本名 | 状态 |
|-----------|-----------|------|
| 一键启动.ps1 | **StartAll.ps1** | ✅ 推荐使用 |
| backend/启动后端.ps1 | **backend/StartBackend.ps1** | ✅ 推荐使用 |
| frontend/启动前端.ps1 | **frontend/StartFrontend.ps1** | ✅ 推荐使用 |

---

## ❓ 常见问题

### Q: 为什么中文脚本报错？
A: PowerShell 在某些系统上无法正确处理 UTF-8 编码的中文字符，请使用英文版脚本。

### Q: 如何停止服务？
A: 在对应终端窗口按 `Ctrl + C`，或直接关闭窗口。

### Q: 前端无法连接后端？
A: 
1. 确认后端已启动（访问 http://localhost:8000/health）
2. 检查 `frontend/.env` 中 API 地址是否为 `http://localhost:8000`
3. 清除浏览器缓存并刷新

---

## 🎯 快速启动命令

```powershell
# 一键启动所有服务
.\StartAll.ps1
```

就这么简单！🎉
